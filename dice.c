#include <stdio.h>
#include <stdlib.h>

int d2(){
	return 1 + (random() % 2);
}

int d3(){
	return 1 + (random() % 3);
}

int d4(){
	return 1 + (random() % 4);
}

int d6(){
	return 1 + (random() % 6);
}

int d8(){
	return 1 + (random() % 8);
}

int d12(){
	return 1 + (random() % 12);
}
int d20(){
	return 1 + (random() % 20);
}

int d100(){
	return 1 + (random() % 100);
}

